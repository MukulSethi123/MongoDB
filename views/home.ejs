<%- include("partials/header") %>
    <div>
        <h1>Home</h1>
        <p>
            <%= homeContent %>
        </p>
        <% posts.forEach(function(post) {%>
            <h1>
                <%=post.title%>
            </h1>
            <p>
                <%= post.body%>
            </p>
            <% }); %>
    </div>
    <%- include("partials/footer") %>